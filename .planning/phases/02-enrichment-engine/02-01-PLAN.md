---
phase: 02-enrichment-engine
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - enrichment/src/types/enriched.ts
  - enrichment/src/types/index.ts
  - enrichment/src/adapters/types.ts
  - enrichment/src/images/resizer.ts
  - enrichment/src/images/__tests__/resizer.test.ts
  - enrichment/src/prompts/enrichment-prompt.ts
  - enrichment/src/prompts/__tests__/enrichment-prompt.test.ts
  - enrichment/src/batch/checkpoint.ts
  - enrichment/src/batch/retry.ts
  - enrichment/src/batch/__tests__/checkpoint.test.ts
  - enrichment/src/batch/__tests__/retry.test.ts
  - enrichment/package.json
autonomous: true
requirements: [ENRC-05, ENRC-06, PIPE-04, PIPE-05]

must_haves:
  truths:
    - "EnrichedFields schema validates the expanded 9+ target fields plus accuracy score"
    - "All adapters can be built against a shared EnrichmentAdapter interface and EnrichmentResult type"
    - "Images can be resized to max 1024px longest edge and base64-encoded on-demand"
    - "Checkpoint files track completed SKUs and survive crash/restart via atomic writes"
    - "Retry utility performs 2x backoff (2s, 5s) before marking failed"
    - "LLM prompt template produces structured instructions for luxury product enrichment"
  artifacts:
    - path: "enrichment/src/types/enriched.ts"
      provides: "Expanded EnrichedFields schema with 9 target fields + accuracy_score"
      contains: "made_in"
    - path: "enrichment/src/adapters/types.ts"
      provides: "EnrichmentAdapter interface, EnrichmentResult type, EnrichmentMetadata type"
      exports: ["EnrichmentAdapter", "EnrichmentResult", "EnrichmentMetadata"]
    - path: "enrichment/src/images/resizer.ts"
      provides: "Image resize to 1024px + base64 encoding"
      exports: ["prepareImageForLLM", "prepareProductImages"]
    - path: "enrichment/src/batch/checkpoint.ts"
      provides: "Checkpoint read/write/resume with atomic writes"
      exports: ["writeCheckpoint", "loadCheckpoint", "getCompletedSkus"]
    - path: "enrichment/src/batch/retry.ts"
      provides: "Retry with 2s/5s backoff"
      exports: ["withRetry"]
    - path: "enrichment/src/prompts/enrichment-prompt.ts"
      provides: "LLM prompt template for product enrichment"
      exports: ["buildEnrichmentPrompt"]
  key_links:
    - from: "enrichment/src/adapters/types.ts"
      to: "enrichment/src/types/enriched.ts"
      via: "EnrichmentResult.fields uses EnrichedFields type"
      pattern: "import.*EnrichedFields.*from.*types"
    - from: "enrichment/src/images/resizer.ts"
      to: "enrichment/src/images/manifest.ts"
      via: "prepareProductImages reads manifest to find local paths"
      pattern: "import.*readManifest.*from.*manifest"
---

<objective>
Create the shared infrastructure for Phase 2 enrichment: expanded schema, adapter interface, image resizer, LLM prompt template, checkpoint/resume module, and retry utility.

Purpose: Every adapter and the batch runner depend on these shared contracts and utilities. Building them first enables parallel adapter development in Wave 2.
Output: Type contracts, image processing, prompt template, checkpoint module, retry utility -- all tested.
</objective>

<execution_context>
@/Users/jadieldossantos/.claude/get-shit-done/workflows/execute-plan.md
@/Users/jadieldossantos/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/02-enrichment-engine/02-CONTEXT.md
@.planning/phases/02-enrichment-engine/02-RESEARCH.md

<interfaces>
<!-- Existing types that this plan extends/uses -->

From enrichment/src/types/enriched.ts (WILL BE MODIFIED):
```typescript
// Current: 6 fields. Must expand to 9+ fields per CONTEXT.md decisions
export const EnrichedFieldsSchema = z
  .object({
    description_eng: z.string().optional(),
    season: z.string().optional(),
    year: z.string().optional(),
    collection: z.string().optional(),
    gtin: z.string().optional(),
    dimensions: z.string().optional(),
  })
  .strict()
```

From enrichment/src/types/product.ts:
```typescript
export type Product = z.infer<typeof ProductSchema>
// Key fields: sku, name, brand, model, color, category, department, images, season, made_in, materials_original, etc.
```

From enrichment/src/images/manifest.ts:
```typescript
export interface ImageManifestEntry {
  readonly url: string
  readonly sku: string
  readonly index: number
  readonly status: 'reachable' | 'unreachable'
  readonly contentType?: string
  readonly fileSize?: number
  readonly localPath?: string
  readonly error?: string
}
export function readManifest(filePath: string): ImageManifestEntry[]
```

From enrichment/src/types/index.ts:
```typescript
export { ProductSchema, type Product } from './product.js'
export { EnrichedFieldsSchema, type EnrichedFields } from './enriched.js'
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Expand EnrichedFields schema and create adapter interface + metadata types</name>
  <files>
    enrichment/src/types/enriched.ts,
    enrichment/src/types/index.ts,
    enrichment/src/adapters/types.ts,
    enrichment/src/adapters/__tests__/types.test.ts
  </files>
  <behavior>
    - Test 1: EnrichedFieldsSchema validates object with all 9 fields (description_eng, season, year, collection, gtin, dimensions, made_in, materials, weight)
    - Test 2: EnrichedFieldsSchema accepts accuracy_score as integer 1-10
    - Test 3: EnrichedFieldsSchema rejects accuracy_score outside 1-10 range
    - Test 4: EnrichedFieldsSchema allows all fields to be undefined (partial enrichment)
    - Test 5: EnrichedFieldsSchema allows additional string fields via passthrough (hybrid approach: allow LLMs to fill extra fields)
    - Test 6: EnrichmentResult type has correct shape (fields, status, fillRate, enrichedFields, accuracyScore, error)
    - Test 7: computeFillRate returns correct percentage for partial enrichment (e.g., 6/9 = 0.67)
    - Test 8: computeFillRate returns 0 for empty fields and 1.0 for all filled
    - Test 9: buildEnrichmentMetadata produces correct metadata object from EnrichmentResult
  </behavior>
  <action>
    1. Update `enrichment/src/types/enriched.ts`:
       - Add `made_in: z.string().optional()`, `materials: z.string().optional()`, `weight: z.string().optional()` to the schema
       - Add `accuracy_score: z.number().int().min(1).max(10).optional()` for LLM confidence
       - Change from `.strict()` to `.passthrough()` per CONTEXT.md hybrid approach (allow LLMs to fill extra fields they discover)
       - Keep all existing 6 fields unchanged
       - Export updated schema and type
       - Add and export `ENRICHMENT_TARGET_FIELDS` constant array with the 9 field names (for fill rate computation)

    2. Update `enrichment/src/types/index.ts`:
       - Add re-exports for ENRICHMENT_TARGET_FIELDS

    3. Create `enrichment/src/adapters/types.ts`:
       - Define `EnrichmentResult` interface: `{ readonly fields: EnrichedFields; readonly status: 'success' | 'partial' | 'failed'; readonly fillRate: number; readonly enrichedFields: readonly string[]; readonly accuracyScore?: number; readonly error?: string }`
       - Define `ImageInput` type: `{ readonly data: Buffer; readonly mimeType: string }`
       - Define `EnrichmentAdapter` interface: `{ readonly name: string; enrich(product: Product, images?: readonly ImageInput[]): Promise<EnrichmentResult> }`
       - Define `EnrichmentMetadata` interface matching the CSV metadata columns: `_enrichment_tool`, `_enrichment_status`, `_enrichment_fill_rate`, `_enriched_fields`, `_enrichment_error`, `_enrichment_accuracy_score`
       - Export `computeFillRate(fields: EnrichedFields): number` -- counts non-undefined, non-empty-string values among the 9 target fields, returns ratio (e.g., 6/9 = 0.67, rounded to 2 decimals)
       - Export `buildEnrichmentMetadata(toolName: string, result: EnrichmentResult): EnrichmentMetadata` -- constructs the metadata object from a result

    4. Create test file `enrichment/src/adapters/__tests__/types.test.ts` with the behavior tests above.
  </action>
  <verify>
    <automated>cd /Users/jadieldossantos/Developer/Lazer/product-enrichment-eval/enrichment && npx vitest run src/adapters/__tests__/types.test.ts src/types/__tests__/product.test.ts -x --reporter=verbose</automated>
  </verify>
  <done>EnrichedFieldsSchema validates 9 target fields + accuracy_score with passthrough for extras. EnrichmentAdapter interface, EnrichmentResult, EnrichmentMetadata, computeFillRate, and buildEnrichmentMetadata are exported and tested. All existing tests still pass.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Image resizer, checkpoint module, retry utility, and LLM prompt template</name>
  <files>
    enrichment/src/images/resizer.ts,
    enrichment/src/images/__tests__/resizer.test.ts,
    enrichment/src/batch/checkpoint.ts,
    enrichment/src/batch/__tests__/checkpoint.test.ts,
    enrichment/src/batch/retry.ts,
    enrichment/src/batch/__tests__/retry.test.ts,
    enrichment/src/prompts/enrichment-prompt.ts,
    enrichment/src/prompts/__tests__/enrichment-prompt.test.ts,
    enrichment/package.json
  </files>
  <behavior>
    - Test 1 (resizer): prepareImageForLLM resizes a buffer and returns { data: string (base64), mimeType: 'image/jpeg' }
    - Test 2 (resizer): prepareProductImages reads manifest for a SKU and returns array of ImageInput buffers (mock sharp and fs)
    - Test 3 (resizer): prepareProductImages returns empty array for products with no reachable images in manifest
    - Test 4 (checkpoint): writeCheckpoint creates valid JSON file (atomic write via temp + rename)
    - Test 5 (checkpoint): loadCheckpoint returns parsed CheckpointData from existing file
    - Test 6 (checkpoint): loadCheckpoint returns undefined for missing file
    - Test 7 (checkpoint): loadCheckpoint returns undefined for corrupt JSON file
    - Test 8 (checkpoint): getCompletedSkus returns Set of completed SKU strings
    - Test 9 (retry): withRetry returns result on first success
    - Test 10 (retry): withRetry retries on failure with correct delays (2s, 5s)
    - Test 11 (retry): withRetry throws after 3 attempts (1 initial + 2 retries)
    - Test 12 (prompt): buildEnrichmentPrompt returns string containing product brand, name, and field instructions
    - Test 13 (prompt): buildEnrichmentPrompt includes existing field values as context (e.g., "season is currently FW23")
    - Test 14 (prompt): buildEnrichmentPrompt includes confidence strategy instructions (conservative for factual, aggressive for generative)
  </behavior>
  <action>
    1. Install sharp: `cd enrichment && npm install sharp && npm install -D @types/sharp`

    2. Create `enrichment/src/images/resizer.ts`:
       - `prepareImageForLLM(localPath: string): Promise<{ data: Buffer; mimeType: string }>` -- reads file, resizes with sharp to max 1024px longest edge (fit: 'inside', withoutEnlargement: true), converts to JPEG quality 85, returns Buffer (NOT base64 -- let adapters handle encoding for their specific API format)
       - `prepareProductImages(sku: string, manifestPath: string, imagesDir: string): Promise<readonly ImageInput[]>` -- reads manifest, filters entries for this SKU with status 'reachable' and localPath defined, calls prepareImageForLLM for each, returns array of ImageInput. Returns empty array if no reachable images.
       - Constants: `MAX_EDGE = 1024`, `JPEG_QUALITY = 85`

    3. Create `enrichment/src/batch/checkpoint.ts`:
       - `CheckpointData` interface: `{ readonly tool: string; readonly startedAt: string; readonly lastUpdatedAt: string; readonly completed: ReadonlyArray<{ readonly sku: string; readonly status: 'success' | 'partial' | 'failed' }> }`
       - `writeCheckpoint(path: string, data: CheckpointData): void` -- write to temp file, then rename (atomic)
       - `loadCheckpoint(path: string): CheckpointData | undefined` -- read and parse, return undefined if missing or corrupt
       - `getCompletedSkus(checkpoint: CheckpointData | undefined): ReadonlySet<string>` -- extract completed SKU set

    4. Create `enrichment/src/batch/retry.ts`:
       - `RETRY_DELAYS = [2000, 5000] as const` (per CONTEXT.md: 2s, 5s)
       - `withRetry<T>(fn: () => Promise<T>, label: string): Promise<T>` -- tries fn, retries on failure with delays, throws after 3 total attempts. Logs retry attempts with label.

    5. Create `enrichment/src/prompts/enrichment-prompt.ts`:
       - `buildEnrichmentPrompt(product: Product): string` -- builds the LLM prompt text. Includes:
         - Product identity: brand, name, model, color, category, department
         - Existing field values as context: "season is currently {value} -- confirm or improve"
         - Field-dependent confidence strategy per CONTEXT.md: "For factual fields (GTIN, dimensions, year, weight): leave blank if uncertain. For generative fields (description_eng, season, collection, materials, made_in): always attempt to fill."
         - Description tone instruction: "Write description_eng as luxury e-commerce copy, 2-3 sentences, professional style (think NET-A-PORTER / SSENSE)"
         - Output format: JSON with the 9 target fields + accuracy_score (1-10 integer)
         - Does NOT include image references (adapters handle that per API format)

    6. Create test files with mocked sharp (vi.mock('sharp')) and mocked fs for resizer tests. Use vi.useFakeTimers() for retry delay tests.
  </action>
  <verify>
    <automated>cd /Users/jadieldossantos/Developer/Lazer/product-enrichment-eval/enrichment && npx vitest run src/images/__tests__/resizer.test.ts src/batch/__tests__/checkpoint.test.ts src/batch/__tests__/retry.test.ts src/prompts/__tests__/enrichment-prompt.test.ts -x --reporter=verbose</automated>
  </verify>
  <done>sharp installed. prepareImageForLLM resizes to 1024px JPEG. prepareProductImages loads manifest and returns ImageInput array. Checkpoint read/write/resume works with atomic writes. withRetry performs 2x backoff (2s, 5s). buildEnrichmentPrompt produces structured prompt with confidence strategy, description tone, and existing values as context. All tests pass.</done>
</task>

</tasks>

<verification>
```bash
cd /Users/jadieldossantos/Developer/Lazer/product-enrichment-eval/enrichment && npx vitest run --reporter=verbose
```
All tests pass including new and existing (84+ existing + new).
```bash
cd /Users/jadieldossantos/Developer/Lazer/product-enrichment-eval/enrichment && npx tsc --noEmit
```
TypeScript compiles cleanly.
</verification>

<success_criteria>
- EnrichedFieldsSchema validates 9 fields + accuracy_score, uses passthrough for extras
- EnrichmentAdapter interface and EnrichmentResult type exported from adapters/types.ts
- computeFillRate and buildEnrichmentMetadata work correctly
- sharp installed, image resizer resizes to 1024px max edge
- Checkpoint module supports atomic write/read/resume
- Retry utility performs 2x backoff (2s, 5s) per CONTEXT.md
- LLM prompt template includes confidence strategy, description tone, and existing values
- All tests pass, tsc --noEmit clean
</success_criteria>

<output>
After completion, create `.planning/phases/02-enrichment-engine/02-01-SUMMARY.md`
</output>
