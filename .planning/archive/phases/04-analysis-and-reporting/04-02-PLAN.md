---
phase: 04-analysis-and-reporting
plan: 02
type: execute
wave: 2
depends_on: ["04-01"]
files_modified:
  - frontend/src/context/ProductContext.tsx
  - frontend/src/App.tsx
  - frontend/src/components/analysis/AnalysisView.tsx
  - frontend/src/components/analysis/AnalysisModeToggle.tsx
  - frontend/src/components/analysis/ExecutiveSummary.tsx
  - frontend/src/components/analysis/WeightControls.tsx
  - frontend/src/components/analysis/FieldWinnerMatrix.tsx
  - frontend/src/components/analysis/CompletenessSection.tsx
  - frontend/src/components/analysis/ExportButton.tsx
  - frontend/src/components/analysis/AnalysisEmptyState.tsx
  - frontend/src/hooks/useAnalysisState.ts
  - frontend/src/lib/analysis/export.ts
autonomous: false
requirements:
  - UI-08
  - UI-09
  - UI-10
  - UI-11
  - UI-12
must_haves:
  truths:
    - "The app has a top-level Compare and Analysis mode switch"
    - "The first analysis view answers which tool wins overall at a glance"
    - "The client can inspect field winners in a matrix-style table"
    - "The client can adjust weights using presets plus manual tuning and see analysis update live"
    - "Completeness metrics show fill rate per tool per field"
    - "CSV export downloads the current analysis view with active filters and weights included"
  artifacts:
    - path: "frontend/src/components/analysis/AnalysisView.tsx"
      provides: "Top-level analysis mode surface wired into the app shell"
    - path: "frontend/src/components/analysis/ExecutiveSummary.tsx"
      provides: "Overall winner, rank cards, and key takeaways"
    - path: "frontend/src/components/analysis/FieldWinnerMatrix.tsx"
      provides: "Matrix table for per-field winner analysis"
    - path: "frontend/src/components/analysis/WeightControls.tsx"
      provides: "Preset and manual weight controls with live recomputation"
    - path: "frontend/src/components/analysis/CompletenessSection.tsx"
      provides: "Per-tool and per-field fill-rate reporting"
    - path: "frontend/src/components/analysis/ExportButton.tsx"
      provides: "CSV export of the current analysis view"
  key_links:
    - from: "frontend/src/components/analysis/AnalysisView.tsx"
      to: "frontend/src/lib/analysis/scoring.ts"
      via: "renders the computed full and filtered analysis summaries"
      pattern: "build.*Analysis|compute.*Ranking"
    - from: "frontend/src/components/analysis/WeightControls.tsx"
      to: "frontend/src/hooks/useAnalysisState.ts"
      via: "preset selection and manual overrides"
      pattern: "setSelectedPreset|setManualWeights"
    - from: "frontend/src/components/analysis/ExportButton.tsx"
      to: "frontend/src/lib/analysis/export.ts"
      via: "serialize current analysis state to downloadable CSV"
      pattern: "downloadAnalysisCsv|createAnalysisCsv"
    - from: "frontend/src/App.tsx"
      to: "frontend/src/components/analysis/AnalysisModeToggle.tsx"
      via: "top-level mode switch in the existing app shell"
      pattern: "AnalysisModeToggle"
---

<objective>
Integrate the tested analysis engine into the frontend with an executive-summary-first dashboard, layered detail sections, live weight tuning, completeness reporting, and CSV export.

Purpose: Deliver the client-facing Phase 4 experience without reopening product decisions. This plan is responsible for presentation, state wiring, and download behavior, not for inventing new ranking math.
Output: A working analysis mode alongside the existing comparison mode, plus a visual checkpoint confirming the dashboard hierarchy and matrix readability.
</objective>

<execution_context>
@/Users/jadieldossantos/.codex/get-shit-done/workflows/execute-plan.md
@/Users/jadieldossantos/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/04-analysis-and-reporting/04-CONTEXT.md
@.planning/phases/04-analysis-and-reporting/04-01-PLAN.md
@frontend/src/App.tsx
@frontend/src/context/ProductContext.tsx
@frontend/src/components/comparison/ComparisonView.tsx
@frontend/src/components/sidebar/ProductSidebar.tsx

<interfaces>
Use the contracts created by Plan 04-01 directly. The UI should render them, not reinterpret them.

Locked product decisions to honor:
- Analysis is a top-level sibling mode to the comparison view, not an inline add-on.
- First screen is executive summary first: overall winner, rank cards, key takeaways.
- Detail order after summary: field winners, completeness, export.
- Dense tool-by-field comparison uses a matrix-style table.
- Weight controls must start with named presets and allow manual tuning.
- Export must reflect the current analysis view, including active filters, weights, rankings, field-winner snapshot, and completeness snapshot.
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Wire the app shell, shared analysis state, and executive summary</name>
  <files>
    frontend/src/context/ProductContext.tsx,
    frontend/src/App.tsx,
    frontend/src/hooks/useAnalysisState.ts,
    frontend/src/components/analysis/AnalysisView.tsx,
    frontend/src/components/analysis/AnalysisModeToggle.tsx,
    frontend/src/components/analysis/ExecutiveSummary.tsx,
    frontend/src/components/analysis/WeightControls.tsx,
    frontend/src/components/analysis/AnalysisEmptyState.tsx
  </files>
  <action>
    Add a top-level Compare / Analysis mode switch to the existing app shell in `frontend/src/App.tsx`. Keep the current product browser and filter system shared across both modes. Do not create a second independent filtering system.

    In `frontend/src/hooks/useAnalysisState.ts`, derive the Phase 4 dashboard state from the existing `ProductContext` data:
    - full-dataset analysis summary,
    - filtered-slice analysis summary,
    - selected weight preset,
    - manual weight overrides,
    - current view scope metadata for export.

    If the filtered slice is empty or if there is no enrichment data, render `AnalysisEmptyState.tsx` with truthful messaging. Do not collapse missing-score tools out of the analysis. Keep them visible and clearly labeled as lacking confidence scores.

    In `frontend/src/components/analysis/AnalysisView.tsx`, establish the analysis page shell and render the first section as the executive summary surface. Leave clear composition points for the later matrix, completeness, and export sections so the file does not absorb all behavior directly.

    In `ExecutiveSummary.tsx`, surface:
    - overall winner headline,
    - ranked tool cards,
    - stable full-dataset result and current filtered result side-by-side or clearly separated,
    - concise takeaways,
    - an explicit note when a tool is evaluated in the no-confidence track.

    In `WeightControls.tsx`, provide:
    - named presets with balanced selected by default,
    - manual tuning inputs for individual fields,
      - live recomputation without a page reload.
  </action>
  <verify>
    <automated>cd frontend && npm run build</automated>
  </verify>
  <done>
    The app shell can switch between Compare and Analysis, the shared analysis state is derived from existing product/filter data, empty states are truthful, and the executive summary plus live weight controls are functional.
  </done>
</task>

<task type="auto">
  <name>Task 2: Add field-winner matrix and completeness reporting to the analysis surface</name>
  <files>
    frontend/src/components/analysis/AnalysisView.tsx,
    frontend/src/components/analysis/FieldWinnerMatrix.tsx,
    frontend/src/components/analysis/CompletenessSection.tsx
  </files>
  <action>
    Extend `frontend/src/components/analysis/AnalysisView.tsx` so the analysis surface unfolds after the summary in the locked order:
    1. Executive summary
    2. Field winner matrix
    3. Completeness section
    4. Export affordance

    In `FieldWinnerMatrix.tsx`, render a dense, scan-friendly matrix table keyed by field and tool. Use the output from Plan 04-01 directly. Support a "too close to call" display instead of forcing a winner.

    In `CompletenessSection.tsx`, show:
    - per-tool overall fill rate,
    - per-field fill rates,
    - any obvious coverage gaps worth calling out in compact summary text.

    Keep both sections presentation-focused. Do not reimplement ranking logic inside the components. Make the matrix readable first, then decorative.
  </action>
  <verify>
    <automated>cd frontend && npm run build</automated>
  </verify>
  <done>
    The analysis dashboard now includes the matrix-style field-winner section and the completeness section after the executive summary, both driven by the shared analysis state.
  </done>
</task>

<task type="auto">
  <name>Task 3: Implement CSV export for the current analysis view</name>
  <files>
    frontend/src/lib/analysis/export.ts,
    frontend/src/components/analysis/ExportButton.tsx
  </files>
  <action>
    Create a client-side export utility that serializes the current analysis view to CSV. The output must include:
    - active filters,
    - active preset and manual weights,
    - full-dataset and filtered ranking snapshot,
    - per-field winner snapshot,
    - completeness snapshot.

    Use clear sectioned rows or a normalized tabular layout, but make the file understandable in spreadsheet tools without hidden assumptions. Prefer stable column names over clever compression.

    Generate the download in-browser. Do not add a backend. Filename should include a timestamp and indicate analysis export.

    The export must reflect the current live analysis state, not a stale full-dataset default. If a field is too close to call, export that exact status instead of forcing a winner.
  </action>
  <verify>
    <automated>cd frontend && npm run build</automated>
  </verify>
  <done>
    Clicking export downloads a CSV representing the active analysis view, including filters, weights, ranking snapshot, field-winner snapshot, and completeness snapshot.
  </done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 4: Visual verification of the analysis dashboard and export flow</name>
  <action>Pause after the analysis UI and export flow are implemented so the user can verify the information hierarchy, matrix readability, live weighting behavior, and export fidelity in the browser.</action>
  <what-built>Top-level analysis mode, executive summary cards, matrix table, weight controls, completeness section, and client-side CSV export.</what-built>
  <how-to-verify>
    1. Run `cd frontend && npm run dev`.
    2. Open the app and switch between Compare and Analysis.
    3. Confirm the Analysis mode starts with the executive summary, not the matrix or export section.
    4. Change filters in the shared sidebar and confirm the filtered analysis updates without losing the stable full-dataset view.
    5. Change the weight preset, then manually tune at least one field, and confirm rankings update live.
    6. Inspect the field-winner matrix and confirm close results can show as "Too close to call".
    7. Confirm FireCrawl and Perplexity remain visible even when they lack confidence data, with clear labeling instead of fake scores.
    8. Export the CSV and confirm it reflects the current filters and weights.
  </how-to-verify>
  <verify>
    <automated>cd frontend && npm run build</automated>
  </verify>
  <done>User confirms the dashboard hierarchy, weight updates, score-track labeling, and CSV export behavior are correct, or returns concrete feedback for revision.</done>
  <resume-signal>Type "approved" if the analysis UI and export look correct, or describe the issues to fix.</resume-signal>
</task>

</tasks>

<verification>
Build the frontend, then complete the visual checkpoint. This plan is not done on build alone because the phase goal depends on information hierarchy and scanability.
</verification>

<success_criteria>
The client can switch into a dedicated Analysis mode, immediately understand the overall winner, inspect per-field and completeness detail, tune weights live, and export the current reporting view to CSV. Missing confidence data remains visible and clearly labeled rather than being silently normalized away.
</success_criteria>

<output>
After completion, create `.planning/phases/04-analysis-and-reporting/04-analysis-and-reporting-02-SUMMARY.md`
</output>
